# Logs
logs
*.log

# Runtime data
pids
*.pid
*.seed

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directory
# Deployed apps should consider commenting this line out:
# see https://npmjs.org/doc/faq.html#Should-I-check-my-node_modules-folder-into-git
#node_modules # see below.

# MongoDB database.
/data

# The config file should not be re-used, as it contains a secret string.
# Remember to create your own from the example!
/config.json

# Build artifacts.
*.tlog
*.pdb

# I used to commit node_modules, but now I commit platform-dependent directories and
# symlink node_modules to them on-the-fly. This is because I want to have all the
# benefits of both worlds. And, guess what, I'm a programmer! So I can.
/node_modules
